import { Form } from './components';

export const App = () => {
  return (
    <div className='app w-screen min-h-screen bg-gray-800 flex justify-center p-6'>
      <Form />
    </div>
  );
};

export default App;
